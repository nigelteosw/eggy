{
  "name": "@eggy/docs",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "test": "bun test",
    "check": "astro check",
    "build": "astro build",
    "validate": "bun scripts/validate-built-site.ts",
    "build:pages": "astro build && bun run validate && rm -rf pages-root && mkdir -p pages-root && cp -R dist/. pages-root/ && touch pages-root/.nojekyll",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^5.9.3"
  }
}
