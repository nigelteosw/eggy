// Package github is the repository adapter for GitHub: it clones a configured
// repository into the runner's workspace and reads files, repository summaries,
// issues, pull request reviews, and check runs. It has no write half.
package github
