# ST1005 forbids capitalized error strings so they read correctly when wrapped
# mid-sentence. Every occurrence here starts with a proper noun -- "Google
# request failed", "Telegram returned HTTP 502" -- which is the documented
# exception to that rule, so the check reports twelve false positives and no
# true ones. Disabled rather than annotated twelve times.
checks = ["inherit", "-ST1005"]
